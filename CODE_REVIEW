This branch makes several repeated mistakes:

* Ignores eval rules
* writes multi-line conditionals inside if/unless parens
* Some subs are too long
* Short 1-line subs should be closer to the top of the file, after hashbase and imports, befor elong ones. init() shold also be closer tot he top, under 1-liners but above other multi-line subs.
* Wrapping of Time::HiRes::time() is done many places but not commented in all of them

Inline comments catch many of these things and provide some further instructions.

Act on all the comments, as well as this file, then delete them in the commit that fixes the issues.

There is also a lot of repeated patterns across the resources, several are mostly identical with a few things changed, can we extract much of that logic into 1 or more roles to avoid repetition?

The Time::HiRes::time wrapping either needs to be done once in a library and imported in all places, or the tests can use Test::MockTime or similar to avoid the wrapping altogether.

There are a few other things done in code to make testing easier, can any of them be handled without changes in the libraries and instead improving the tests?

After fixing all the items from this code review and writing a commit, all the code added in this branch needs to be audited agains the style guide and any fixes in an additional commit after the code review fixes.

Neither this file or the code review comments should remain once the issues are fixed.
